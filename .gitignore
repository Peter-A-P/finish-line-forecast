# Private working notes, never committed
CLAUDE.local.md

# Secrets
.env
.env.*

# Fetched pages, entrant lists and the resolved history cache: personal data, never committed
data/cache/
data/raw/
data/entrants/
data/resolved/

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
build/
*.egg-info/

# OS and editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# The snapshot log: a local record of a scheduled job, not data
data/entrants/snapshot.log

# Observed weather: Open Government Licence data, but a big local cache, not committed
data/cache/eccc/
/site/

# The hand-labelled resolution pairs (names as printed): kept local until it is decided
# whether a labelled sample of named people is committed (docs/todo.md item 6)
data/labels/
